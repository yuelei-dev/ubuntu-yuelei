# ===== 安全红线：绝不上传 =====
# 浏览器登录态（含抖音 cookie，泄露=账号被盗）
browser_data/
*_user_data_dir/
**/dy_user_data_dir/

# 真实客户名单 / 爬取原始数据（含 PII：昵称/抖音号/IP属地）
data/
*.jsonl
leads_*.md
leads_*.xlsx
*对标情报报告*

# 密钥 / 凭据
*.enc
secrets*
.env
*.key
config.local.*

# Python
.venv/
__pycache__/
*.pyc

# 系统
.DS_Store
*.log

# 任务数据库(含爬取结果PII)
jobs.db
server/jobs.db
*.db
*.db-wal
*.db-shm
*.sqlite
models/
transcribe_out/
# 生成产物落盘目录（图片/视频/音频/参考素材）。跑一次本地校验就会往里写文件，
# 之前 22 个 cine_ref_*.png / motion_ref_*.mp4 就是这么被 git add -A 扫进 PR 的。
server/content_out/
.gstack/

# 口型 Provider PoC 的真实媒体、报告和供应商响应（只保留目录占位）
/tools/lipsync_poc/reports/*
!/tools/lipsync_poc/reports/.gitkeep
/.local-content-out/lipsync-poc/

# ===== 临时/派生：截图预览、原始大图、本地工具缓存（不进 git，可重生成）=====
.playwright-cli/
.context/
**/_preview/
site/assets_raw/
workbench/assets_raw/

# ===== Cloud Design 落地：工作源 / 大图 / 本地配置（不进 git，可重生成）=====
site/_cloud_src/
site/_logo_gen/
generated_layers/
config/
docs/黄雀网站主站图预览（对标）/

# 设计源料 / 生成图临时件 / 大参考图（不进库）
System design polish*/
黄雀主站生成图片实例/
site/assets/cloud/_ref3.png

# 本地预览/原型页（不部署）
site/_*.html

# Tang 本地实验/散件(不进 git)
git管理/
/dashboard.html
/dashboard_data.json
/ai-engineering-concepts.md
pinterest美女图片/

# 本地开发密钥（勿提交）
secret.local.env
*.local.env
server/content.env

# Hermes 运行数据、登录态与第三方凭据
server/hermes_ip12/data/
server/hermes_ip12/media_library/
server/hermes_ip12/knowledge/
server/hermes_ip12/.agnes_key
server/hermes_ip12/agnes_key.txt
server/hermes_ip12/*cookies*.txt
server/hermes_ip12/backups/
server/hermes_ip12/*.bak*
server/hermes_ip12/*_backup.py
server/hermes_ip12/nohup.out
