# 黄雀生产环境 · 完整清单与从零还原手册

> 服务器：腾讯云 `dapeng-server`（129.204.166.13）｜线上：https://huangquechuanmei.com
> 目的：把生产环境**完整记录在 git**，做到「checkout + 照本手册 = 还原整套生产」。3 人协作的单一事实源。
> 最后核对：2026-06-29（Claude 审计）

---

## 一、服务总览（systemd 单元都在 `deploy/systemd/`）

| 服务 | 端口 | 工作目录 | 跑什么 | EnvironmentFile | 代码在 git |
|---|---|---|---|---|---|
| `huangque-auth` | 8095 | /home/ubuntu/auth-service | `auth_server.py` + `hq_cli_api.py` + `invites.py` + `invite_network.py` + `business_cards.py` | — | `server/auth_server.py` + `server/hq_cli_api.py` + `server/invites.py` + `server/invite_network.py` + `server/business_cards.py` ✅ |
| `huangque-invite-reward-claims.timer` | — | /home/ubuntu/auth-service | 每分钟调用 `process_invite_reward_claims.py`，批量处理到期邀请奖励 | — | `scripts/process_invite_reward_claims.py` + 对应 systemd service/timer ✅ |
| `huangque-content` | 8096 | /home/ubuntu/content-api | `content_api.py`（作图gpt/文案/配音 CosyVoice/任务核心） | `content.env` | `server/content_api.py` ✅ |
| `huangque-dl` | 8097 | /home/ubuntu/dl-service | `dl_service.py` | — | `server/dl_service.py` ✅ |
| `huangque-leadgen-api` | 8100 | /home/ubuntu/content-api | `leadgen_api.py` | `content.env` | `server/leadgen_api.py` ✅ |
| `huangque-imggen-api` | 8101 | /home/ubuntu/content-api | `imggen_api.py`（nano banana） | `content.env` | `server/imggen_api.py` ✅ |
| `hermes-ip12-preview` | 3102（回环） | /home/ubuntu/hermes-web | Hermes Flask 全量路由（主站 IP12、报告、图像/视频、Agnes、团队工作台） | `~/.secrets/hermes-openai.env` | `server/hermes_ip12/` ✅ |
| `leadgen-A`（旧获客） | 8091 | /home/ubuntu/leadgen-A | MediaCrawler venv 跑 `app:app` | — | `server/app.py`（待核对一致性） |
| `leadgen-B`（旧获客） | 8092 | /home/ubuntu/leadgen-A | 同上（第二实例） | — | 同上 |
| `xiaotan`（深采） | 8501 | /home/ubuntu/douyin-scraper | `start.py` | — | 上游工具，见下 |

> 共用基础设施：`content_jobs.db`（任务库）、`users.db`（点数）、`content_out/`（出图）都在 `/home/ubuntu/content-api/`。轮询/历史/文件由 8096 统一服务。

---

## 二、上游工具（**不 vendor 进 git** · 太大且含登录态）

| 工具 | 用途 | 目录 / 大小 | 来源 | 部署 |
|---|---|---|---|---|
| **MediaCrawler** | 旧获客发现层（关键词搜+评论采集），leadgen-A/B 跑它的 venv | `/home/ubuntu/MediaCrawler` · **1.2G**（含 `browser_data/` cookie、`data/` PII） | https://github.com/NanmiCoder/MediaCrawler | clone + `pip install` + 配 `config/`（cookie 不进 git） |
| **小探 douyin-scraper** | 深采层（账号深采+下载+口播ASR） | `/home/ubuntu/douyin-scraper` · 173M | https://github.com/Evil0ctal/Douyin_TikTok_Download_API | clone + venv + `config.yaml`（不进 git） |

> ⚠️ 这俩**绝不进 git**：体积大 + 含 cookie/PII。只记来源+版本+部署方式。我们对它们的封装/编排在 `server/app.py`（getleads 后端）、`scripts/`。

---

## 三、不进 git 的（红线 · 已 gitignore）

- **密钥**：`/home/ubuntu/content-api/content.env`（600）、`/home/ubuntu/.secrets/hermes-openai.env`（600）、Hermes 的 `.agnes_key`、`/etc/nginx/.htpasswd-hq`、`~/.heygen/credentials` 等 → 只在服务器，值绝不进 git。
- **数据库**：`*.db`（content_jobs/users，含 PII）、MediaCrawler `data/`。
- **登录态**：`browser_data/`、`*_user_data_dir/`（抖音 cookie）。
- **缓存/产物**：`content_out/`、`tikhub_cache.db`、`__pycache__`。
- **服务器上的 `content_api.py.bak.*`（24 个）**：同事直接改服务器留下的时间戳备份 → 不进 git。**根治办法见第六节。**

---

## 四、从零还原生产（runbook）

```bash
# 1. 拉代码
git clone <repo> && git checkout <统一主分支>

# 2. 自有后端（5个）逐个就位 + systemd
rsync server/auth_server.py server/hq_cli_api.py server/wechat_subscribe.py server/invites.py server/business_cards.py    dapeng:/home/ubuntu/auth-service/
rsync server/content_domains/__init__.py server/content_domains/cos.py server/content_domains/miniprogram_security.py server/content_domains/pricing.py dapeng:/home/ubuntu/auth-service/content_domains/
rsync server/content_api.py server/leadgen_api.py server/imggen_api.py server/tikhub.py  dapeng:/home/ubuntu/content-api/
rsync server/dl_service.py     dapeng:/home/ubuntu/dl-service/

# ⚠ 单元 + drop-in 一起拷。只拷 *.service 会漏掉 drop-in：
#   imggen 的 auth.env 只在 huangque-imggen-api.service.d/internal-token.conf 里挂载，
#   漏了它 → HQ_INTERNAL_TOKEN 缺失 → 退点全部 500，用户的点退不回来（该服务没有直写兜底）。
sudo cp deploy/systemd/*.service /etc/systemd/system/
sudo cp -r deploy/systemd/*.service.d /etc/systemd/system/
sudo rm -f /etc/systemd/system/*.service.d/*.example      # .example 只是模板，不落地
sudo systemctl daemon-reload
sudo systemctl enable --now huangque-{auth,content,dl,leadgen-api,imggen-api}

# 3. 填密钥（手动，不在 git）
#    /home/ubuntu/content-api/content.env  ← TIKHUB_KEY / GEMINI_API_KEY / OPENAI_API_KEY / DASHSCOPE_API_KEY / 代理
#    /home/ubuntu/auth-service/auth.env    ← HQ_INTERNAL_TOKEN（auth/content/imggen/leadgen 共用同一个）
#    /etc/nginx/.htpasswd-hq               ← 团队密码
#
#  密钥一律走 EnvironmentFile 指向 600 的 env 文件，不要写进 systemd 单元的 Environment=。
#  单元文件默认 644，谁都能读；CI 门禁(ci_validate.py)会拦住带明文密钥的 deploy/systemd/**。

# 4. 上游工具（旧获客/深采，按需）
#    clone MediaCrawler → /home/ubuntu/MediaCrawler，部署 leadgen-A（app.py=server/app.py），enable leadgen-A/B
#    clone douyin-scraper → /home/ubuntu/douyin-scraper，enable xiaotan

# 5. nginx（仅在 PR 合并后，从 main 的 deploy 正本手工发布）
python3 -m unittest discover -s tests -p 'test_nginx*.py' -v
HQ_NGINX_ACTIVE=/etc/nginx/sites-available/huangquechuanmei
HQ_NGINX_BACKUP=/etc/nginx/sites-available/huangquechuanmei.rollback
sudo install -m 0644 "$HQ_NGINX_ACTIVE" "$HQ_NGINX_BACKUP"
sudo install -m 0644 deploy/nginx-huangquechuanmei.conf "$HQ_NGINX_ACTIVE"

# reload 前置门禁：失败时恢复旧配置，运行中的 nginx 不受影响
if ! sudo nginx -t; then
  sudo install -m 0644 "$HQ_NGINX_BACKUP" "$HQ_NGINX_ACTIVE"
  sudo nginx -t
  exit 1
fi
sudo systemctl reload nginx

# reload 后冒烟：普通健康接口仍可用，伪造内部 Token 也只能得到 404
curl -fsS https://huangquechuanmei.com/api/auth/health
for HQ_INTERNAL_PATH in \
  /api/auth/points \
  /api/auth/points/deduct \
  /api/auth/points/refund \
  /api/auth/membership/voice-slot-entitlement \
  /api/auth/admin/points/adjust \
  /api/auth/admin/points/audit \
  /api/auth/admin/users \
  /api/auth/admin/password/reset \
  /api/auth/admin/recharge/review \
  /api/auth/admin/recharge/orders
do
  test "$(curl -sS -o /dev/null -w '%{http_code}' \
    -H 'X-HQ-Internal-Token: forged' \
    "https://huangquechuanmei.com$HQ_INTERNAL_PATH")" = 404 || exit 1
done

# 任一 reload 后冒烟失败时回滚
# sudo install -m 0644 "$HQ_NGINX_BACKUP" "$HQ_NGINX_ACTIVE"
# sudo nginx -t && sudo systemctl reload nginx

# 6. 自检
curl https://huangquechuanmei.com/api/gen/health
```

邀请奖励到期处理上线后还必须确认定时器已启用，并检查最近一次执行结果：

```bash
systemctl is-enabled huangque-invite-reward-claims.timer
systemctl is-active huangque-invite-reward-claims.timer
systemctl list-timers huangque-invite-reward-claims.timer --no-pager
journalctl -u huangque-invite-reward-claims.service -n 20 --no-pager
```

### Hermes IP12 完整服务

服务器的 `/home/ubuntu/.secrets/hermes-openai.env` 必须明确配置
`HERMES_DEPLOY_USER=ubuntu` 和 `HERMES_DEPLOY_GROUP=ubuntu`。发布脚本会用该账号、
用户组及 `0700` 权限创建私有备份目录。若回滚任一步骤失败，脚本以 `125`
退出并输出 `manual recovery required`、备份路径和失败步骤；此时不得继续发布，
须按输出的备份路径人工恢复并重新执行健康检查。

```bash
# 只从待发布的确切 Git commit 导出，避免把本机 ignored 运行数据带上服务器。
HERMES_SHA=$(git rev-parse HEAD)
HERMES_STAGE=$(mktemp -d)
HERMES_REMOTE_STAGE="/tmp/hermes-ip12-release-${HERMES_SHA}-$(date +%Y%m%d%H%M%S)-$$"
git archive "$HERMES_SHA" -- \
  server/hermes_ip12 \
  scripts/migrate_hermes_artifacts.py \
  deploy/hermes-ip12-release.sh \
  deploy/systemd/hermes-ip12-preview.service \
  deploy/nginx-hermes-ip12-direct.conf \
  deploy/nginx-huangquechuanmei.conf | tar -x -C "$HERMES_STAGE"
test -f "$HERMES_STAGE/scripts/migrate_hermes_artifacts.py"
test -f "$HERMES_STAGE/deploy/hermes-ip12-release.sh"
python3 -m py_compile "$HERMES_STAGE/scripts/migrate_hermes_artifacts.py"
chmod 0755 "$HERMES_STAGE/deploy/hermes-ip12-release.sh"
ssh dapeng-server "mkdir -p '$HERMES_REMOTE_STAGE'"
rsync -a "$HERMES_STAGE/" "dapeng-server:$HERMES_REMOTE_STAGE/"

# 覆盖前备份代码、systemd/nginx 配置和服务状态。发布脚本从停机开始
# 安装统一 EXIT trap；迁移、rsync、依赖、配置或任一健康检查失败都会
# 自动恢复旧代码/配置、daemon-reload，并恢复发布前的服务状态。
ssh dapeng-server \
  "HERMES_SHA='$HERMES_SHA' HERMES_RELEASE_DIR='$HERMES_REMOTE_STAGE' \
  bash '$HERMES_REMOTE_STAGE/deploy/hermes-ip12-release.sh'"
```

Hermes 的运行数据保留在 `/home/ubuntu/hermes-web/data/`、`media_library/`、`knowledge/`；部署和回滚代码时都不得覆盖或删除这些目录。

```bash
# 精确回滚上一次 Hermes 发布；只删除新版代码文件，明确排除运行数据和密钥。
ssh dapeng-server 'bash -s' <<'REMOTE'
set -euo pipefail
backup=$(cat /home/ubuntu/hermes-last-backup)
case "$backup" in /home/ubuntu/deploy-backups/hermes-*) ;; *) exit 2 ;; esac
test -d "$backup/code"
sudo systemctl stop hermes-ip12-preview.service || true
rsync -a --delete \
  --exclude data/ --exclude media_library/ --exclude knowledge/ \
  --exclude .agnes_key --exclude agnes_key.txt --exclude '*cookies*.txt' \
  --exclude backups/ --exclude '*.log' --exclude nohup.out --exclude '*.bak*' --exclude '*_backup.py' \
  --exclude __pycache__/ --exclude '*.pyc' \
  "$backup/code/" /home/ubuntu/hermes-web/
restore_file() {
  saved="$1"; state="$2"; target="$3"
  if test "$(cat "$state")" = present; then
    sudo install -m 0644 "$saved" "$target"
  else
    sudo unlink "$target" 2>/dev/null || true
  fi
}
restore_file "$backup/hermes-ip12-preview.service" "$backup/hermes-ip12-preview.service.state" /etc/systemd/system/hermes-ip12-preview.service
restore_file "$backup/nginx-hermes-ip12-direct.conf" "$backup/nginx-hermes-ip12-direct.conf.state" /etc/nginx/sites-available/hermes-ip12-direct
restore_file "$backup/nginx-huangquechuanmei.conf" "$backup/nginx-huangquechuanmei.conf.state" /etc/nginx/sites-available/huangquechuanmei
restore_file "$backup/nginx-huangquechuanmei-enabled.conf" "$backup/nginx-huangquechuanmei-enabled.conf.state" /etc/nginx/sites-enabled/huangquechuanmei
if test "$(cat "$backup/hermes-ip12-direct.enabled")" = present; then
  sudo ln -sfn /etc/nginx/sites-available/hermes-ip12-direct /etc/nginx/sites-enabled/hermes-ip12-direct
else
  sudo unlink /etc/nginx/sites-enabled/hermes-ip12-direct 2>/dev/null || true
fi
sudo systemctl daemon-reload
sudo nginx -t
if test "$(cat "$backup/hermes-ip12-preview.service.state")" = present; then
  case "$(cat "$backup/hermes-ip12-preview.enabled")" in
    enabled) sudo systemctl enable hermes-ip12-preview.service ;;
    *) sudo systemctl disable hermes-ip12-preview.service ;;
  esac
  if test "$(cat "$backup/hermes-ip12-preview.active")" = active; then
    sudo systemctl restart hermes-ip12-preview.service
  else
    sudo systemctl stop hermes-ip12-preview.service
  fi
else
  sudo systemctl disable --now hermes-ip12-preview.service 2>/dev/null || true
fi
if test "$(cat "$backup/hermes-ip12-preview.active")" = active; then
  sudo systemctl is-active --quiet hermes-ip12-preview.service
  curl -fsS http://127.0.0.1:3102/healthz >/dev/null
fi
sudo systemctl reload nginx
REMOTE
```

---

## 四-A、CosyVoice 音频发布与回滚

- 新版首次启动会把 4 个公共音色的 `provider_voice` 幂等迁移为 CosyVoice 预置名并清空旧供应商试听；已有试听但遗留为 `training` 的 CosyVoice 个人槽位会按现行判据修正为 `ready`，个人音色内容和历史音频资产不改。
- 回滚窗口结束前保留服务器受保护环境中的旧 `DOUBAO_*` 配置；删除仓库 `.example` 不会自动删除真实 drop-in。
- 如果要把代码回滚到豆包版本，先停 `huangque-content`，备份 `audio_assets.db`，再执行下面的反向迁移；`preview_*` 保持空值，让旧代码重新生成试听：
- 下面的 SQL 只还原 4 个公共音色。个人 CosyVoice 音色不能转换成豆包音色；回滚后仍需保留 `DASHSCOPE_API_KEY` 和兼容 CosyVoice 的代码，否则个人音色不可用。

```bash
python3 - <<'PY'
import sqlite3

db = "/home/ubuntu/content-api/audio_assets.db"
voices = {
    "S_d21F8OR62": "longwan",
    "S_l8wE8OR62": "longxiaochun",
    "S_pa0E8OR62": "longcheng",
    "S_xaUB8OR62": "longxiaoxia",
}
with sqlite3.connect(db) as conn:
    for legacy, preset in voices.items():
        conn.execute(
            """UPDATE audio_voices
               SET provider_voice=?, preview_file=NULL, preview_url=NULL
               WHERE scope='public' AND username='' AND voice_key=?
                 AND provider_voice=?""",
            (legacy, legacy, preset),
        )
PY
```

---

## 四-B、漂移哨兵与部署保护

- 哨兵脚本：`scripts/drift_sentinel.py` → `/home/ubuntu/hq-drift/drift_sentinel.py`。
- 巡检基准：直接比较线上运行文件与服务器 checkout 的 `origin/main`，不再依赖上次 `--bless` 的线上快照。
- 合法部署：`./ship` 在 rsync 后会执行 `drift_sentinel.py --verify-deploy <本次文件>`，确认本次部署文件线上内容等于 git。
- 部署记录：`./ship` 只用 `--bless-deploy <本次文件>` 记录本次文件清单，不会把整站当前线上状态吞进基线。
- 服务器 checkout：`./ship` 会给 `/home/ubuntu/huangque-main-site` 安装 pre-commit 拦截钩子，禁止在服务器 checkout 直接提交热改。

手动巡检：

```bash
python3 /home/ubuntu/hq-drift/drift_sentinel.py --print
```

## 四-C、测试服事务发布的合并拓扑门禁

- 仓库统一只使用普通 merge commit，GitHub 仓库设置必须保持 `merge commit` 开启、`squash merge` 和 `rebase merge` 关闭。
- 合并命令必须锁定独立审核通过的精确 Head：`gh pr merge <PR> --merge --match-head-commit <审核Head>`。
- 合并后、服务器写入前必须同时锁定远端 `main`，并验证审核 Head 是合并提交的祖先：

```bash
REVIEWED_HEAD=<40位审核Head>
MERGED_MAIN=$(git ls-remote origin refs/heads/main | awk '{print $1}')
git fetch origin "$MERGED_MAIN"
git merge-base --is-ancestor "$REVIEWED_HEAD" "$MERGED_MAIN"
```

- 上述校验失败时不得部署，不得把 `--reviewed-source-commit` 改成 base、squash 提交或其他祖先来绕过门禁；应新建独立 PR 修复提交拓扑，并重新通过同提交 CI 与发布审核。
- 服务器发布仍由仓库锁定的事务执行器完成；执行器继续校验干净 `main`、本地 `origin/main`、实时远端 `main`、源文件哈希、线上 preimage、备份、postimage、单次重启和健康检查。
- 发布记录必须保存审核 Head、合并后 `main`、实际部署文件、备份路径、重启次数、健康结果和回滚状态。

---

## 五、当前已知问题（验证于 2026-06-29，待 3 人协作解决）

1. **分支分叉**：新后端在 `design-sync`（领先 main 106）；`main` 反向多 8 个（旧获客/worker/服务器归档/图片库）。**没有任一分支能完整还原生产** → 需合并成单一主分支并设为默认。
2. **部署配置漂移**：leadgen-A/B、xiaotan 的 systemd 单元之前不在 git（**本次已收进 `deploy/systemd/`**）。
3. **同事直接改服务器**：`content_api.py` 在服务器上有 24 个 `.bak` 备份 = 没走 git。**协作规则要求：一律先 pull→改→push→部署，禁止直接改服务器。** 见 `docs/团队Git协作规矩.md`。

---

## 五-B、DNSPod WebBlock 单次只读检测

`scripts/domain_access_check.py` 每次只执行一次 HTTP 首跳和 HTTPS 最终状态检测，输出 JSON；它不会修改 DNS/Nginx、自动修复或持续监控。

```bash
python3 scripts/domain_access_check.py --domain huangquechuanmei.com --timeout 10
```

- 退出码 `0` 表示本次检测通过；`1` 表示命中 WebBlock、异常跳转、HTTPS 异常或网络错误。
- 应从无 hosts 覆盖、实际受影响的网络运行；单次 `OK` 不能证明间歇问题已消失。
- 命中 `DNSPOD_WEBBLOCK` 时，保存检测时间、网络地区和 JSON 结果，再到腾讯云核对备案及当前接入关系。服务器或解析地址以实时清单为准，不在此处写死。

---

## 六、3 人协作规则（基于本清单）

- **单一事实源 = git 统一主分支**；服务器只是"跑正本的地方"。
- 改任何后端：`git pull → 改 → commit → push → 部署`（部署前再 pull）。**禁止直接改服务器**。
- 各管各的文件（获客/下载/作图 = Tang；content_api 音频/CosyVoice = 同事；前端 site/workbench）。
- 改共用库表结构（users/jobs）先打招呼。
- AI agent 也按团队成员处理，必须先读 `AGENTS.md` 和 `docs/团队Git协作规矩.md`；部署记录要包含 commit、文件、服务、验证结果。
- 详见 `docs/团队Git协作规矩.md` + `docs/后端架构与API.md`。
