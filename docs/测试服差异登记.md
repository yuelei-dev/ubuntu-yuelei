# 小磊开发服（8.148.158.106）差异登记与部署治理

> 生效日期：2026-08-03（v2 修订）｜ 维护人：开发负责人 ｜ 复审：每次基线升级后
> 盘点依据：`小磊开发服盘点报告-20260731.md`（二、三轮审查定稿）
> 对账清单：`docs/测试服漂移清单-20260803.md`（逐文件归属，可复核）

## 一、基线声明

测试服运行基线 = **tang 生产主线 @ `54c4d2b`（2026-07-31）冻结快照**。

- 基线冻结：**tang 仓库与生产服务器均不修改**（负责人 2026-08-03 指示），不再安排从 tang main 升级；功能增量仅从 yuelei 仓库部署
- 基线镜像：`/opt/tang-repository`（只读参照，经 bundle 通道同步，服务器不存 tang 凭据）
- yuelei 仓库镜像：`/opt/huangque-repository`（已装 pre-commit 禁提交钩子），用于 yuelei PR 工作
- 部署方式：`ship --exact-files`（精确文件模式，PR #174 引入）+ drift_sentinel --verify-deploy，禁止手工拷贝；**默认模式的 content_domains 整目录 `--delete` 同步在测试服一律禁止**（会覆盖本节登记的例外）

## 二、已登记例外（相对基线 54c4d2b）

### 2.1 yuelei 版本文件（6 个）——基线冻结，保留现状

| 文件 | 归属 | 现状裁决 | 备注 |
|---|---|---|---|
| server/content_domains/video.py | yuelei 版 | 保留现状 | 含 wg_red 调用路径（半上线，见 2.5） |
| server/content_domains/video_seedance.py | yuelei 版 | 保留现状 | PR #169 已合并（2026-08-01，ea4bd70） |
| server/content_domains/breakdown.py | yuelei 版 | 保留现状 | PR #168 已合并（98e7a9d） |
| server/content_domains/feature_flags.py | yuelei 版 | 保留现状 | Seedance 通道默认开启 |
| site/workbench/script.html | yuelei 版 | 保留现状 | 编导页面 |
| scripts/drift_sentinel.py | yuelei 版 | 保留现状 | 哨兵本体，与 tang 版有差异 |
| server/content_domains/local_reverse_processor.py | yuelei 独有 | 保留 | tang 无此路径（哨兵 added 项） |

### 2.2 非常规文件（1 个）

| 文件 | 归属 | 裁决 |
|---|---|---|
| server/content_domains/core.py | yuelei PR171 测试覆盖层（e755f40） | 保留；如需还原为基线版本，单独走 yuelei PR + 回归 |

### 2.3 缺失项（相对 54c4d2b，10 项）

HQ CLI 发布产物（site/downloads/hq/*）、site/workbench/device.html、site/workbench/canvas/canvas-agent.js、server/content_domains/canvas_agent.py——均未部署到测试服。**裁决：基线冻结，不补部署。**

### 2.4 滞留旧版（37 项）

5 个 py（admin_api.py、tikhub.py、audio.py、history.py、registry.py——registry 与 yuelei main 内容相同）+ 32 个前端文件停留在 tang 7-29 版（496b51b1）。逐文件清单见 `docs/测试服漂移清单-20260803.md`。**裁决：基线冻结，维持现状；如某个文件因 yuelei 功能需要更新，单独走 yuelei PR + `ship --exact-files`。**

### 2.5 已知半上线功能

wg_red 网感字幕（yuelei PR #92）：服务器为 yuelei 版 video.py + tang 版前端，前端无入口，正常用户不可达；直接 API 调用会以"ASR 识别失败"报错退点（talking_asr_cli.py 缺失）。**裁决：维持现状，是否补齐由产品决策，不占治理流程。**

## 三、部署登记

- 命名约定（沿用既有惯例）：`/opt/huangque-deployments/pr<号>-<sha>-<时间戳>` 与 `/opt/huangque-deploy-backups/` 同名备份
- 每次部署走 `ship --exact-files`（精确文件模式；content_domains 一律精确模式，禁止整目录 `--delete`），完成后执行：`drift_sentinel.py --verify-deploy <文件>` 通过后 `--bless-deploy <文件>`（记录写入 `/home/ubuntu/hq-drift/deploy_bless.jsonl`）
- PR 预览目录：统一 `pr<号>-<sha8>` 命名；只保留进行中的 PR，合并/关闭即清理
- 部署记录必须包含：commit SHA、文件清单、服务、操作者、关联 PR/任务号

## 四、漂移监控

- 哨兵：`/home/ubuntu/hq-drift/drift_sentinel.py`，比对 `/opt/tang-repository` 的 main
- 定时任务：`/etc/cron.d/huangque-drift`，每小时 17 分 `--print` 记录（例外机制上线前不告警，避免已知漂移刷屏）
- 待办：哨兵增加例外清单支持（登记为例外 ≠ 漂移）后切告警模式
- 已知基线差（对 54c4d2b，共 55 处，逐文件对账见 `docs/测试服漂移清单-20260803.md`）：changed 44 = 滞留旧版 37 + yuelei 版本 6 + PR171 覆盖层 1；missing 10；added 1。对当前 tang main 为 148 处（含主线升级积压，基线冻结后不再追逐）

## 五、红线

- 禁止直接改服务器运行文件；所有改动走 分支→PR→`ship --exact-files`
- **tang 仓库与生产服务器（129.204.166.13）均不修改**
- content_domains 禁止整目录 `--delete` 同步（登记例外会被覆盖）
- 密钥、数据库、用户数据不进 git、不进登记文档
