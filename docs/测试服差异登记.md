# 小磊开发服（8.148.158.106）差异登记与部署治理

> 生效日期：2026-08-03 ｜ 维护人：开发负责人 ｜ 复审：每次基线升级后
> 盘点依据：`小磊开发服盘点报告-20260731.md`（二、三轮审查定稿）

## 一、基线声明

测试服运行基线 = **tang 生产主线**（tang730125633/huangque-main-site）。

- 当前部署基线：tang@`54c4d2b`（2026-07-31）干净部署
- 基线镜像：`/opt/tang-repository`（本地分支 main，经 bundle 通道同步，服务器不存 tang 凭据）
- 升级方式：定期从 tang main 升级，升级走 ship + drift_sentinel --verify-deploy，禁止手工拷贝
- yuelei 仓库镜像：`/opt/huangque-repository`（已装 pre-commit 禁提交钩子），用于 yuelei PR 工作

## 二、已登记例外（相对基线 54c4d2b）

### 2.1 yuelei 功能差异（6 个文件）——升级 tang main 前必须逐个裁决

| 文件 | 归属 | 现状裁决 | 备注 |
|---|---|---|---|
| server/content_domains/video.py | yuelei 版 | 保留现状，升级前 diff 裁决 | 含 wg_red 调用路径（半上线） |
| server/content_domains/video_seedance.py | yuelei 版 | 保留现状，升级前 diff 裁决 | PR #169 在途 |
| server/content_domains/breakdown.py | yuelei 版 | 保留现状，升级前 diff 裁决 | PR #168 已合入 yuelei main |
| server/content_domains/feature_flags.py | yuelei 版 | 保留现状，升级前 diff 裁决 | |
| server/content_domains/registry.py | yuelei 版 | 保留现状，升级前 diff 裁决 | |
| server/content_domains/local_reverse_processor.py | yuelei 独有 | 保留 | tang 无此路径 |

### 2.2 非常规文件（1 个）

| 文件 | 归属 | 裁决 |
|---|---|---|
| server/content_domains/core.py | yuelei PR171 测试覆盖层（e755f40） | 保留至下次基线升级；PR171/172 已合并，升级时还原为 tang 版本并回归 |

### 2.3 缺失项（相对 54c4d2b，10 项）

HQ CLI 发布产物（site/downloads/hq/*）、site/workbench/device.html、site/workbench/canvas/canvas-agent.js、server/content_domains/canvas_agent.py——均未部署到测试服。**裁决：不单独补部署，随下次 tang main 升级一并落地。**

### 2.4 滞留旧版（32 项）

4 个 py（admin_api.py、tikhub.py、audio.py、history.py）+ 28 个前端文件停留在 tang 7-29 版（496b51b1）。**裁决：不单独处理，随下次 tang main 升级解决。**

### 2.5 已知半上线功能

wg_red 网感字幕（yuelei PR #92）：服务器为 yuelei 版 video.py + tang 版前端，前端无入口，正常用户不可达；直接 API 调用会以"ASR 识别失败"报错退点（talking_asr_cli.py 缺失）。**裁决：维持现状，是否补齐由产品决策，不占治理流程。**

## 三、部署登记

- 命名约定（沿用既有惯例）：`/opt/huangque-deployments/pr<号>-<sha>-<时间戳>` 与 `/opt/huangque-deploy-backups/` 同名备份
- 每次部署后执行：`drift_sentinel.py --verify-deploy <文件>` 通过后 `--bless-deploy <文件>`（记录写入 `/home/ubuntu/hq-drift/deploy_bless.jsonl`）
- PR 预览目录：统一 `pr<号>-<sha8>` 命名；只保留进行中的 PR，合并/关闭即清理
- 部署记录必须包含：commit SHA、文件清单、服务、操作者、关联 PR/任务号

## 四、漂移监控

- 哨兵：`/home/ubuntu/hq-drift/drift_sentinel.py`，比对 `/opt/tang-repository` 的 main
- 定时任务：`/etc/cron.d/huangque-drift`，每小时 17 分 `--print` 记录（例外机制上线前不告警，避免已知漂移刷屏）
- 待办：哨兵增加例外清单支持（登记为例外 ≠ 漂移）后切告警模式
- 已知基线差：对 54c4d2b 55 处（39 例外 + 10 未部署 + 哨兵自身）；对当前 tang main 148 处（另有主线升级积压）

## 五、红线

- 禁止直接改服务器运行文件；所有改动走 分支→PR→ship
- 密钥、数据库、用户数据不进 git、不进登记文档
- 生产服务器（129.204.166.13）永久只读，本登记不适用于生产
