# 私域批量成片 UI 验收证据

- `desktop-1280.png`: 1280px 桌面视口，入口、批量文案、首帧预览和配置区均可见。
- `mobile-390.png`: 390px 移动视口，无横向溢出（`scrollWidth == clientWidth == 390`）。
- 交互检查：切换“女性成长·温暖”模板、随机更换素材和音乐、生成两条批量配置方案。
- 浏览器控制台：以上流程无 JavaScript error。
- 当前边界：此 PR 不提交付费渲染任务；页面明确显示该限制。
